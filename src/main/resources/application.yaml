spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rgr
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: always
      continue-on-error: false
      schema-locations: classpath:db/schema.sql

  web:
    resources:
      static-locations: classpath:/static/

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB

  #
  # mail:
  #   username: ${MAIL_USERNAME}
  #   password: ${MAIL_PASSWORD}
  #   host: smtp.gmail.com
  #   port: 587
  #   properties:
  #     mail:
  #       smtp:
  #         auth: true
  #         starttls:
  #           enable: true
  #           required: true
  #         connectiontimeout: 5000
  #         timeout: 5000
  #         writetimeout: 5000


  mail:
    host: localhost
    port: 1025
    from: noreply@localhost
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

security:
  jwt:
    secret: ${JWT_SECRET:0720640ed6ea9a449aa75e54a5c0b788a92fae60a2eb006ff1e8e15de7d968a9=}
    access: 1        # 1 час
    refresh: 30      # 30 дней

app:
  name: Sicelica
  base-url: http://localhost:8080
  upload:
    dir: uploads
    max-size: 52428800   # 50 MB